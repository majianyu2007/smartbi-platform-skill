{
  "name": "smartbi-platform-skill",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "doctor": "node scripts/install.mjs --check",
    "install:playwright": "node scripts/install.mjs --install-playwright",
    "test": "node --test tests/*.test.mjs"
  }
}
